# config

Centraliza leitura de configuracao.

- `config.yaml` mapeia chaves esperadas.
- `.env` fornece valores por ambiente e nao deve ser commitado.
- `.env.example` documenta variaveis obrigatorias.
