# migrations

O MVP usa `GORM AutoMigrate` no bootstrap para manter a execucao local simples.

Esta pasta guarda a documentacao SQL da estrutura esperada e deve receber migrations versionadas quando o projeto sair de MVP.
